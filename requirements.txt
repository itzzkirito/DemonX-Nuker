# Core dependencies - pinned versions for reliability
discord.py==2.3.2
httpx==0.25.0
colorama==0.4.6
aiohttp==3.9.0

# UI enhancements (optional - has fallback)
rich>=13.0.0
pyfiglet>=0.8.0

# Build dependencies
pyinstaller==5.13.0

